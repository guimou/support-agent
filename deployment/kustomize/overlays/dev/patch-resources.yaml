apiVersion: apps/v1
kind: Deployment
metadata:
  name: litemaas-agent-proxy
spec:
  template:
    spec:
      containers:
        - name: proxy
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
