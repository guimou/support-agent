apiVersion: apps/v1
kind: Deployment
metadata:
  name: litemaas-agent-proxy
spec:
  template:
    spec:
      containers:
        - name: proxy
          resources:
            requests:
              cpu: 500m
              memory: 1Gi
            limits:
              cpu: 2000m
              memory: 2Gi
